import { makeStyles } from "@mui/styles";

export default makeStyles({
  appPage: {
    display: "flex",
    width: "100vw",
    height: "100vh",
    position: "fixed",
    overflowY: "hidden",
  },
});
